<?xml version="1.0" encoding="UTF-8"?>
<schema xmlns="http://www.w3.org/2001/XMLSchema"
		xmlns:mc160="http://fmk-teknik.dk/160"
		targetNamespace="http://fmk-teknik.dk/160"
		elementFormDefault="qualified" 
		attributeFormDefault="unqualified">
		
	<complexType name="PartOfBatchOperationFlagType">
		<annotation>
			<documentation xml:lang="en-GB">Flag to be set in request header, in case the requested service is a batch operation where extended validation in general should be disabled, in order to avoid extend validation to act as blocking errors (reserved for future use)</documentation>
			<documentation xml:lang="da-DK">Flag der kan anvendes i request headeren for at angive, at en operation skal ses som en del af en batch operation, og derved skal udvidede valideringer ikke længere foretages (reserveret til fremtidig brug)</documentation>
		</annotation>
	</complexType>	
</schema>
